# 频道讨论区 + 禁言 + 进群验证（手动设置）

本项目的推送机器人跑在 GitHub Actions 上，**不能 24 小时在线守群**，所以：
- 资讯推送：用本仓库 bot
- 进群加减法验证：用现成验证机器人（免费）

---

## 1. 做成「每条可评论」：频道关联讨论群

频道本身订阅者不能随便发帖；关联讨论群后，每条频道消息会出现 **评论**，成员在讨论群里聊。

### 步骤

1. Telegram → **新建群组**（建议公开），例如：`rix科技风向标-讨论`
2. 打开你的频道 → **Edit channel**
3. 点 **Discussion / 讨论** → **Add a group** → 选刚建的讨论群
4. 保存

之后频道每发一条，底下会出现评论入口。

### 「全员禁言」怎么理解

| 地方 | 怎么设 |
|------|--------|
| **频道** | 默认只有管理员能发，无需再禁言 |
| **讨论群** | 若希望「只能评论频道帖、不能随便开话题」：群权限里关掉普通成员「发消息」以外不必要的权限；或用验证机器人管理。一般讨论群要允许发言，否则没法评论 |

推荐：频道只发资讯；讨论群允许发言（可开验证防广告）。

---

## 2. 进群人机验证（加减法）——用现成机器人

自己写验证需要 **长期在线的服务器**；你现在是白嫖 Actions，请直接用成熟 bot：

### 推荐：Shieldy（常见、支持算术验证）

1. 打开 Telegram 搜索：`@shieldy_bot`（或当前可用的 Shieldy 官方 bot）
2. 把 bot **拉进讨论群**，设为 **管理员**（至少：删除消息、限制用户）
3. 在群里按 bot 提示 `/start` 或打开设置
4. 打开 **Captcha / 验证**，类型选 **数学题 / Arithmetic**（如 `3+5=?`）
5. 未通过验证的用户限制发言或踢出

同类可选（界面略有不同，都支持进群验证）：

- `@Combot`
- `@MissRose_bot`
- `@GroupHelpBot`

选一个即可，**不要叠太多**，容易冲突。

---

## 3. 推送机器人权限（资讯 bot）

`@rix_new_admin_bot` 只需在 **频道** 当管理员并开启 **Post Messages**。  
一般 **不必** 把推送 bot 也做成讨论群验证 bot（职责分开更稳）。

---

## 4. 检查清单

- [ ] 频道已关联讨论群（Edit → Discussion）
- [ ] 讨论群已加验证 bot 并设管理员
- [ ] 验证类型为加减法/算术
- [ ] 频道推送 bot 仍能正常发帖
